import { BrowserRouter, Routes, Route } from "react-router-dom";

function Router() {
  return (
    <BrowserRouter>
      <Routes>{/* 여기서 페이지 라우터 관리 */}</Routes>
    </BrowserRouter>
  );
}

export default Router;
